<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:sicaeestacionamiento}
                ?useSSL=false&amp;serverTimezone=America/Mexico_City"/>
                <property name="username" value="${DB_USER:root}"/>
                <property name="password" value="${DB_PASSWORD:1234}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="mybatis/mappers/EspacioEstacionamientoMapper.xml"/>
        <mapper resource="mybatis/mappers/MovimientoMapper.xml"/>
    </mappers>
</configuration>